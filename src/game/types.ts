/**
 * Type definitions for the fish game.
 * 
 * Types:
 * - FishType: 'player' | 'enemy'
 * - Personality: 'lazy' | 'aggressive' | 'erratic'
 * - Position: { x: number, y: number }
 * - Velocity: { x: number, y: number }
 * - GameState
 * - PlayerState
 * - InputState
 */

export type FishType = 'player' | 'enemy';
// Additional types coming soon 